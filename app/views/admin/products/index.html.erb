<div id="products" style="padding: 30px 0">
    <div class="container">

        <div class="row">
            <div class="col-lg-12">
                <h3>商品</h3>
            </div>
        </div>

        <div class="row">
            <div class="col-md-12">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>名称</th>
                                <th>组合</th>
								<th>义卖</th>
                                <th>图片</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            <% @products.order(:id).each do |product| %>
                            <tr>
                                <td><%= link_to product.name, edit_admin_product_path(product) %></td>
                                <td>
                                    <% product.collections.each do |collection| %>
                                        <%= collection.name %><br/>
                                    <% end %>
                                </td>
								<td>
                                    <% if product.is_featured %>
                                    <span class="glyphicon glyphicon-ok"></span>
                                    <% end %>
								</td>
                                <td>
                                    <%= cl_image_tag product.picture, width: 50, height: 50, crop: :limit, class: 'img-thumbnail' if product.picture.present? %>
                                </td>
                                <td>
                                    <%= link_to '删除', admin_product_path(product), method: :delete, data: { confirm: '您确定要删除该商品吗?' } %>
                                </td>
                            </tr>
                            <% end %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>

        <%= link_to '添加商品', new_admin_product_path, class: 'btn btn-primary' %>

    </div>
</div>
